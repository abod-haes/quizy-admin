# Leave empty when the admin UI and Nest API are served from the same origin.
# API endpoint constants already include /api/v1/admin, so do not append /api here.
VITE_API_BASE_URL=

VITE_DEFAULT_LOCALE=ar
VITE_APP_ENV=local
